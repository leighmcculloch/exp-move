script {

    //use 0x2::Coin;
    //use 0x2::Drip;
    //use 0x3::Fun::FunCoin;

    fun drip(_s: signer) {
        //let c = Coin::get<FunCoin>(&s);
        // Drip::lockup(&s, c, Coin::split);
        // TODO: drip
    }

}
